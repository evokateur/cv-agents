# Optimizer-specific configuration for agent models and temperatures
# Override locally by creating settings.local.yaml (gitignored)
# Shared settings (RAG, chat, paths) are in src/config/settings.yaml

agents:
  cv_analyst:
    model: "gpt-4o-mini"
    temperature: 0.7
  job_analyst:
    model: "gpt-4o-mini"
    temperature: 0.7
  cv_strategist:
    model: "claude-3-5-sonnet-20241022"
    temperature: 0.7
  cv_rewriter:
    model: "claude-3-5-sonnet-20241022"
    temperature: 0.7
  crew_manager:
    model: "gpt-4o-mini"
    temperature: 0.7
